package com.example.sportlink.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Material3 color scheme for SportLink application.
 * Modern blue-violet color palette with professional, energetic colors.
 */

// Primary colors - Blue-violet palette
val Primary = Color(0xFF636CCB) // Violet-blue (636CCB)
val PrimaryVariant = Color(0xFF50589C) // Medium blue (50589C)
val Secondary = Color(0xFF6E8CFB) // Light blue (6E8CFB)
val SecondaryVariant = Color(0xFF3C467B) // Dark blue (3C467B)

// Background colors
val Background = Color(0xFFF5F7FA) // Soft gray-white
val Surface = Color(0xFFFFFFFF) // Pure white
val SurfaceVariant = Color(0xFFE8EBF5) // Light blue tint

// Text colors
val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFFFFFFFF)
val OnBackground = Color(0xFF1A1A1A) // Almost black
val OnSurface = Color(0xFF1A1A1A)

// Error colors
val Error = Color(0xFFE53935) // Modern red
val OnError = Color(0xFFFFFFFF)

// Additional colors
val Outline = Color(0xFFB0BEC5) // Light gray-blue
val OutlineVariant = Color(0xFFE0E0E0) // Very light gray

// Sport-specific accent colors (using new palette)
val SportBlue = Color(0xFF636CCB) // Primary violet-blue
val SportBlueLight = Color(0xFF6E8CFB) // Light blue
val SportBlueMedium = Color(0xFF50589C) // Medium blue
val SportBlueDark = Color(0xFF3C467B) // Dark blue

// Gradient colors
val GradientStart = Color(0xFF636CCB) // Violet-blue
val GradientEnd = Color(0xFF6E8CFB) // Light blue

